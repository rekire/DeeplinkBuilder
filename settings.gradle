pluginManagement {
    repositories {
        mavenLocal() // just for plugin development
        mavenCentral()
        maven { url "https://plugins.gradle.org/m2/" }
    }
}

rootProject.name = 'DeeplinkBuilder'

include 'gradle-plugin', 'sample-app'